# CAP-2044 — Pay Bills: validate amount before submitting a payment

**Type:** story · **Source:** mock · **URL:** —

## Summary

The Pay Bills one-time payment form needs to give customers clear inline feedback when they enter an unusable amount — non-numeric text, zero/negative values, or an amount that exceeds the selected account's balance — instead of letting the submission proceed with an unclear result, while still letting a valid, in-balance amount go through as it does today. This applies whether the form is reached directly or via a Saved Payee's quick-pay button, since quick-pay only preselects a payee and scrolls/focuses the same form.

## Acceptance Criteria

| ID | Testable | Text |
|----|----------|------|
| AC-1 | ✅ | Given the customer is on the Pay Bills page, when they submit the one-time payment form with a non-numeric amount (e.g. "abc"), then they should see an inline error and no payment should go through. |
| AC-2 | ✅ | Given the customer enters an amount of $0 or a negative number, then they should see an inline error and no payment should go through. |
| AC-3 | ✅ | Given the customer enters an amount greater than the available balance of the selected 'Pay from' account, then they should see a clear insufficient funds error and no payment should go through. |
| AC-4 | ✅ | Given the customer enters a valid amount within their balance, then the payment should submit successfully as it does today. |

## Requirements

| ID | Type | Related AC | Text |
|----|------|-----------|------|
| REQ-1 | functional | AC-1 | Submitting the one-time payment form with a non-numeric amount value displays an inline error message near the amount field. |
| REQ-2 | functional | AC-1 | Submitting the one-time payment form with a non-numeric amount value does not create a payment transaction or debit any account. |
| REQ-3 | functional | AC-2 | Submitting the one-time payment form with an amount of $0 or a negative number displays an inline error message. |
| REQ-4 | functional | AC-2 | Submitting the one-time payment form with an amount of $0 or a negative number does not create a payment transaction or debit any account. |
| REQ-5 | functional | AC-3 | Submitting an amount greater than the selected 'Pay from' account's available balance displays an insufficient-funds-specific inline error, distinguishable from the generic invalid-amount error. |
| REQ-6 | functional | AC-3 | Submitting an amount greater than the selected 'Pay from' account's available balance does not create a payment transaction or debit any account. |
| REQ-7 | functional | AC-4 | Submitting a valid amount that is greater than $0 and does not exceed the selected account's balance completes the payment, debits the account, and shows the existing payment-complete confirmation. |
| REQ-8 | functional | — | Amount validation behaves identically regardless of whether the one-time payment form was reached by clicking a Saved Payee's quick-pay button or by scrolling to the form directly, since quick-pay only preselects the payee and focuses the shared form. |

## Edge Cases

- **EC-1** — Amount entered with more than 2 decimal places (e.g. "40.999"), as raised in dev1's comment.
  _Rationale: Neither the ACs nor the current implementation (src/data.js parseAmount, src/api/accountsApi.js payBill) round or reject extra decimal precision — the value is passed straight through as a float. Left untested, a fractional-cent payment could silently submit with no defined expected behavior._
- **EC-2** — Submitting the form with the amount field left empty.
  _Rationale: src/data.js parseAmount('') returns NaN, which follows the same code path as non-numeric input in accountsApi.js payBill (the '!amount || Number.isNaN(amount) || amount <= 0' check). This is the same behavior as AC-1 but isn't explicitly called out, so it's worth a dedicated boundary check rather than assuming it's covered by the non-numeric case._
- **EC-3** — Amount entered exactly equal to the selected account's available balance.
  _Rationale: accountsApi.js payBill only rejects when 'amount > from.balance', so an exact-balance payment should succeed — AC-3 only specifies amounts 'greater than' the balance, leaving this boundary (amount === balance) unstated and worth explicit verification._
- **EC-4** — Amount entered with currency formatting such as "$1,234.56", surrounding whitespace ("  100  "), or thousands separators.
  _Rationale: src/data.js parseAmount strips '$', ',', and whitespace before parsing, so these should be accepted as valid amounts, not rejected as 'non-numeric' the way AC-1 describes for actual letters — this distinction isn't spelled out in the ACs and could be misread as invalid input._
- **EC-5** — Account balance changes between page load and submit (e.g. a payment completes in another tab first), as raised by qa1's comment.
  _Rationale: accountsApi.js payBill re-reads balance from storage at submit time via loadState(), so the balance check itself is likely already fresh, but no AC or existing test (PayBills.test.jsx) confirms what message the customer sees in this scenario, or whether it's meant to look identical to the standard insufficient-funds error._

## Open Ambiguities

- **Q-1** — 🟢 not blocking — Should the amount field cap or reject more than 2 decimal places (e.g. "40.999"), as dev1 asked in comments? This isn't in the acceptance criteria.
  _Impact: Without an answer, the Design Agent either skips decimal-precision testing entirely (leaving a known gap the dev flagged) or invents a rounding/rejection rule the product owner never specified, which could produce a test that fails against intended behavior or asserts on behavior nobody asked for._
- **Q-2** — 🟢 not blocking — Is handling a stale/changed account balance between page load and submit (concurrent payment in another tab) in scope for this ticket, as qa1 asked, or is it a separate ticket?
  _Impact: If left unresolved, the Design Agent may either design a test plan around a scenario product never intended to be covered by this ticket, or omit a legitimate race-condition gap entirely — either way the test scope won't match what's actually expected to be verified here._
- **Q-3** — 🔴 **BLOCKING** — The ticket description says the amount field 'currently... lets a customer submit whatever they type,' but src/api/accountsApi.js payBill already rejects non-numeric, zero/negative, and over-balance amounts with inline errors (role="alert"), and src/pages/PayBills.test.jsx already has a passing test for the insufficient-funds case. It's unclear whether the reporter is unaware this validation already exists, or whether the real ask is a different validation flow (e.g. earlier/inline-as-you-type client-side validation) than the existing submit-time check.
  _Impact: If unresolved, the Design Agent could either treat AC-1/AC-2/AC-3 as new functionality to build test coverage for from scratch (when much of it may just need regression confirmation), or miss that the real intended change is a different validation timing/UX than what's already implemented, changing what the tests should actually assert on._

**Gate status:** 🔴 blocked on Q-3 — do not proceed to Design Agent until answered.

## Target Area

**Pages:** PayBills

Verified against C:\Users\sumit.kumar1\Documents\bank-app-1\src\pages\PayBills.jsx and the sibling files it depends on: C:\Users\sumit.kumar1\Documents\bank-app-1\src\api\accountsApi.js (payBill function, lines ~160-196) and C:\Users\sumit.kumar1\Documents\bank-app-1\src\data.js (parseAmount, lines 73-78). Notably, non-numeric/zero/negative-amount and insufficient-funds validation already exist server-side in payBill and are already surfaced as an inline role="alert" error in PayBills.jsx — see Q-3. Existing coverage in C:\Users\sumit.kumar1\Documents\bank-app-1\src\pages\PayBills.test.jsx already exercises the happy path and the insufficient-funds case; it does not cover non-numeric input, zero/negative amounts, decimal-place precision, or the quick-pay entry point's validation equivalence (REQ-8).

---
*Generated from `CAP-2044.requirements.json` by scripts/render-plan.mjs — that file is the canonical machine-readable artifact; this file is a generated human-readable rendering. Do not hand-edit; re-run the renderer instead.*
